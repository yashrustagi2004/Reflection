apiVersion: monitoring.coreos.com/v1
kind: ServiceMonitor
metadata:
  name: qa-generation-servicemonitor
  namespace: reflection
  labels:
    app: qa-generation
    release: prometheus
spec:
  selector:
    matchLabels:
      app: qa-generation
  namespaceSelector:
    matchNames:
      - reflection
  endpoints:
    - port: http
      path: /metrics
      interval: 15s
      scrapeTimeout: 10s
      scheme: http
      honorLabels: true
      # Optional relabeling to reduce cardinality or drop sensitive labels
      metricRelabelings: []
